<h1><%= set_title(t('posts.editing')) %></h1>

<%= render 'entries/form', entry: @entry %>

<%= button_to 'Destroy this bookmark', @entry.post, class: "btn btn-danger", data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>

<br />

<%= link_to 'Show', @entry.post %> |
<%= link_to 'Back', posts_path %>
