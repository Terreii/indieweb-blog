<%= render "static_pages/me" %>

<h2>📨 <%= t 'general.latest_entries' %></h2>

<div class="h-feed">
  <span class="p-name hidden">Main Feed</span>

  <% @entries.each do |entry| %>
    <%= render entry %>
  <% end %>
</div>
